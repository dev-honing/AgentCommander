{
  "_comment": [
    "앱의 상태(state)와 glb 안의 애니메이션 클립 이름을 잇는 표.",
    "clip 값은 frontend/lib/protocol.ts 의 STATE_CLIP 과 일치해야 한다.",
    "match 는 FBX 파일명에서 찾을 키워드(소문자, 부분 일치). 위에서부터 먼저 맞는 것을 쓴다.",
    "새 애니메이션을 추가하려면 여기에 항목만 넣으면 된다 — 코드는 건드릴 필요 없다."
  ],
  "clips": [
    {
      "clip": "Idle",
      "state": "idle",
      "match": ["breathing idle", "idle"],
      "required": true
    },
    {
      "clip": "Working",
      "state": "running",
      "match": ["typing", "working", "type"],
      "required": false
    },
    {
      "clip": "LookAround",
      "state": "waiting",
      "match": ["looking around", "look around", "lookaround", "searching"],
      "required": false
    },
    {
      "clip": "Retry",
      "state": "retrying",
      "match": ["thinking", "head shake", "shake", "retry"],
      "required": false
    },
    {
      "clip": "Alert",
      "state": "error",
      "match": ["defeated", "dismissing", "alert", "disappoint"],
      "required": false
    },
    {
      "clip": "Cheer",
      "state": "done",
      "match": ["cheering", "cheer", "waving", "wave", "clapping"],
      "required": false
    }
  ]
}
